# requirements-locked.txt — exact package versions from the paper's working pod
#
# Python: 3.10.12
# CUDA:   12.8
# torch / torchvision / torchaudio are NOT listed here.
# Install them first from requirements-torch-cu128.txt:
#
#   pip install -r requirements-torch-cu128.txt
#   pip install -r requirements-locked.txt
#
# Or use the reproducible setup script:
#
#   RESET_ENV=1 bash setup_repro_env.sh

# ── HuggingFace stack ─────────────────────────────────────────────────────────
transformers==4.57.6
datasets==3.6.0
accelerate==1.14.0
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub

# ── Numerics ──────────────────────────────────────────────────────────────────
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
scikit-learn

# ── Utilities ─────────────────────────────────────────────────────────────────
pyyaml==6.0.3
tqdm
psutil
sentencepiece
matplotlib
