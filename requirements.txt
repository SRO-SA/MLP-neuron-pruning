# ── Python dependencies for qwen_swiglu_pruning ──────────────────────────────
#
# This file covers only normal Python packages.
# PyTorch (torch, torchvision, torchaudio) must be installed separately via:
#
#   bash setup_env.sh   (recommended — handles CUDA detection and ABI matching)
#
# or manually:
#   pip install -r requirements-gpu-cu128.txt   (stable CUDA 12.8)
#
# ── HuggingFace stack ─────────────────────────────────────────────────────────
#
# Qwen3-30B-A3B (Qwen3MoeForCausalLM) was added in transformers 4.51.0.
# The upper bound <5.0.0 guards against breaking API changes in a future major.
#
transformers>=4.51.0,<5.0.0
accelerate>=0.34.0,<2.0.0
datasets>=2.20.0,<4.0.0
huggingface_hub>=0.24.0,<2.0.0
safetensors>=0.4.5,<1.0.0
tokenizers>=0.19.0,<1.0.0
sentencepiece>=0.1.99,<0.3.0

# ── Numerics / ML utilities ───────────────────────────────────────────────────
numpy>=1.26.0,<3.0.0
scipy>=1.12.0,<2.0.0           # used for ridge regression helpers
pandas>=2.2.0,<3.0.0
scikit-learn>=1.4.0,<2.0.0

# ── Visualisation / reporting ─────────────────────────────────────────────────
matplotlib>=3.9.0,<4.0.0

# ── System / CLI utilities ────────────────────────────────────────────────────
tqdm>=4.66.0,<5.0.0
pyyaml>=6.0,<7.0
psutil>=5.9.0,<7.0.0
